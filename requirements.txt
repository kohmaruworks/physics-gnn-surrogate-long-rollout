# act / basic / phase2 と同じ方針: リポルートで uv venv → uv pip install -r requirements.txt
# torch CPU のみなら（必要時）: uv pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
numpy>=1.24
torch>=2.0
torch-geometric>=2.4.0
